---
import cn from 'classnames'

import s from './Breadcrumbs.module.css'

interface Props {
  breadcrumbs: string[]
}

const { breadcrumbs } = Astro.props
---

<span class={s.breadcrumbs}>
  {breadcrumbs.join(' / ')}
</span>
